# アーキテクチャ

*[English](architecture.md) | [日本語](architecture.ja.md)*

`board-identify` は 3 つの同一性を区別します。

- **ポート**: `/dev/ttyUSB2` のような、一時的なカーネルノード。
- **トランスポート**: CH340、FTDI、WCH-Link デバッグプローブのような USB インタフェースまたは USB-UART ブリッジ。
- **ターゲット**: トランスポートの先にあるマイコンボード。

1 つのポートに名前を 1 つだけ付けるとは限りません。単なる USB-UART ブリッジ自体には見るべきものがありませんが、デバッグプローブは違います。固有のシリアル番号を持ち、デバッグ端子の先のボードは差し替えられます。そのためプローブは識別結果のリストを、具体的なものから順に返し、その一つ一つが同じポートを指すリンクになります。

パブリッシャは識別結果ごとに `/run/board-identify/by-id/` へアトミックにシンボリックリンクを作成し、その一組を一時的なポート名をキーにして状態として保存します。

## モジュール

| モジュール | 責務 |
| --- | --- |
| `paths` | ランタイムの配置。`by-id/` のリンクと `state/` のファイル。 |
| `model` | プローブ 1 回の結果である `Identification`。 |
| `normalize` | チップ名や固有 ID を安全な構成要素へ正規化する。 |
| `usbinfo` | ポートを開かずに sysfs から読む USB のメタデータ。 |
| `usb_ids` | VID/PID の組み合わせから何を読み取ってよく、何を読み取ってはならないか。 |
| `arduino_ids` | 生成物。インストール済みの Arduino ボード定義が名乗る組み合わせ。 |
| `probes/` | ターゲットファミリごとのクラス。`identify_port()` が選択する。 |
| `identify` | プローブの振り分け、`publish()`、`remove_port()`。 |
| `cleanup` | 切断されたデバイスが残したリンクと状態の掃除。 |
| `cli` | `identify`、`remove`、`cleanup` の各サブコマンド。 |

## ランタイムの配置

```text
/run/board-identify/
├── by-id/
│   ├── esp32-s3-7cdfa1123456 -> /dev/ttyUSB2
│   ├── ch32x035c8t6-1ff9abcd880ebc48 -> /dev/ttyACM4
│   └── wch-link-fc928f068181 -> /dev/ttyACM4
└── state/
    ├── ttyACM4.json
    └── ttyUSB2.json
```

リンクはボード、状態ファイルはポートをキーとし、1 つの状態ファイルが複数のリンクを持ちます。

```json
{
  "port": "/dev/ttyACM4",
  "board_ids": [
    "ch32x035c8t6-1ff9abcd880ebc48",
    "wch-link-fc928f068181"
  ],
  "identifications": [ ... ]
}
```

どちらも一時的な名前で書き出してから `os.replace()` で所定の位置へ移動するため、読み手が中途半端なリンクや書きかけの状態ファイルを見ることはありません。

## ライフサイクル

1. udev が新しい `ttyUSB*` / `ttyACM*` ノードを検出し、`board-identify@<port>.service` を起動する。
2. `identify_port()` が各プローブに対応可否を尋ね、対応するものに識別を依頼する。
3. `publish()` が識別結果ごとのリンクと状態ファイルを書き出す。前回は名乗っていたが今回は名乗らなかった名前は先に削除するため、デバッグプローブから外されたターゲットのリンクが残ることはない。
4. デバイスが外れると `BindsTo=` によりユニットが停止され、その `ExecStop=` が `board-identify remove <port>` を実行する。
5. `board-identify cleanup` は、再起動や異常終了などで取り残されたリンクと状態をさらに掃除する。

## ディスクリプタのテーブル

`arduino_ids` は USB の VID/PID を `(family, platform, variant)` へ対応付けます。`arduino-cli board details` を公開ダンプした [`board_details.json`](https://tanakamasayuki.github.io/arduino-cli-helper/board_details.json) から `scripts/generate_usb_ids.py` がマージし、コミットしているものです。

ローカルの Arduino インストールを読むのが一見自然ですが、それは二重に誤りです。`~/.arduino15` にボードが現れるのはそのコアをインストールした後であり、識別したいボードとはまだセットアップしていないボードそのものです。加えて識別は udev から動き、そこでの `$HOME` は何かをインストールしたユーザではありません。どのマシンの、どのユーザのもとで挿されたかによって名前が変わってはならないからです。

3 つのフィールドのうち 2 つが実際に働きます。

- `variant` はボードの名前で、複数のボードが同じ組み合わせを共有する場合は None になります。
- `family` はより粗い主張で、名前がないときにも効くのはこちらです。他ファミリに属する組み合わせは Espressif のターゲットではありえないので、`EspressifProbe` はそれを確かめるためにボードをリセットせず、ポートを辞退します。

マージは追加のみです。既にテーブルにあるエントリは手で直されている可能性があり、また既に公開されたボードは upstream が改名しても同じ名前を保たなければならないので、生成側は追加しかしません。書き出し時に VID → PID でソートするため、手編集と機械マージがファイル上で衝突しません。

ボードのファミリは、FQBN が指すプラットフォームのアーキテクチャから決めます。シリコンではなくコアや OS の名前が付いたアーキテクチャ（`mbed`、`zephyr`、`host`）もファミリを生みますが、同じ組み合わせを名乗る他の主張と食い違ったときは負けます。Seeed XIAO nRF52840 は `mbed` と `nrf52` の両方でパッケージされていて、チップを言い当てているのは後者だからです。

Arduino 由来ではない知識は `usb_ids` が持ちます。汎用 USB-UART ブリッジの ID — CH340、CP2102、FT232、PL2303 — が示すのはケーブルなので、マージ時に除外し、さらに `board_for_usb_id()` でも拒否します。この 2 段目は冗長ではありません。将来のソースが汎用ブリッジの ID を名乗った状態でマージしても、その先のすべてのボードを CH340 に代弁させないための備えです。除外は意図的に両方向に対称で、そうした組み合わせはボードを命名もせず、プローブを除外もしません。ESP32 の前段に CH340 がいる構成こそ、本プロジェクトが存在する理由だからです。

このガードは今まさに効いています。備えではありません。Sony Spresense は CP2102 の標準 ID `10c4:ea60` を自分のものとして登録しており、除外しなければマシン上のすべての CP2102 ボードが Spresense として公開され、`esptool` から遠ざけられます。代償は、本物の Spresense がディスクリプタ由来の名前を失い `esptool` の接続試行を受けることで、比べるまでもなく安いほうの誤りです。

Espressif と他ファミリの両方が名乗る組み合わせは、解決せずに除外します。テーブルから外すことが、両者を見分けられる唯一の手段である `esptool` にポートを開けておくことになるからです。

## デバッグプローブ

デバッグプローブは、自分の先に何が付いているかを知っているトランスポートなので、2 段階で識別します。

1. **ディスクリプタから、常に。** sysfs から VID/PID とシリアル番号が、USB 通信も tty のオープンもなしに取れます。これがプローブ自身の名前になります。この段階は失敗しませんし、何も乱しません。
2. **ターゲットから、指示されたときだけ。** プローブのベンダインタフェースを claim して短いコマンド列を流すと、接続されているチップのシグネチャと工場出荷時の UUID が得られ、これがボードの名前になります。`--no-target-probe` はこの段階を丸ごと省くため、USB 通信はまったく発生しません。

3 つ目の段階は必要なときだけ走ります。ツールによっては、**プローブ側**がターゲットの壊れた読み取り値を抱えたまま残ります。family バイトは正しいままですが、チップ ID と UUID が 4 バイトの同じ語の繰り返しになり、attach をやり直しても直りません。detach でも、ターゲットの電源を入れ直しても解消しません。おかしいのはボードではなくプローブだからです。この読み取り値を公開するのは、何も公開しないより悪いことです。でたらめな UUID はその状態にあるすべてのボードで同一だからです。そこでシグネチャがどのチップにも解決しなかった場合には `81 0d 01 03` でプローブに見直させ、もう一度読みます。`wlink reset` でも解消しますが、それは再読み取りを強制する副作用にすぎません。デバッグモジュールのスティッキーな `havereset` ビットで計測したところ、`81 0b 01 01` はターゲットをリセットし、`81 0d 01 03` はリセットしません。復旧にはリセットしないほうを使うので、識別が WCH ターゲットをリセットすることは依然としてありません。

プローブ自身のリンク名をあえてディスクリプタだけから作っているのは意図的です。WCH-Link がベンダインタフェース越しに報告する型名のほうが良い名前ですが、それは既にデバッグセッションがそのインタフェースを掴んでいると取得できません。他に誰が動いているかで変わる名前は、安定した名前ではありません。型名はトランスポートとして記録します。

## 標準の仕組みを使わない理由

`/dev/serial/by-id/`、`/dev/serial/by-path/`、`ATTRS{idVendor}` / `ATTRS{idProduct}` / `ATTRS{serial}` を照合する udev ルールは、使えるなら常にそちらが正解であり、接続時のコストもかかりません。本プロジェクトが扱うのは、その残りの領域です。

- シリアル番号を報告しないアダプタ（多くの CH340 モジュールなど）。`by-id` では複数のボードが 1 つの名前に潰れてしまいます。
- USB/IP で WSL へ転送されたデバイス。Windows 側のバス ID は Linux から見えず、`by-path` は物理的な接続位置ではなく `vhci_hcd` への attach 順を反映します。

どちらの場合も、attach し直しても変わらない識別子はターゲット自身から得るしかありません。プローブが読み取るのはそれです。

## 既知の制約

- ターゲットと通信すれば、ターゲットは乱されます。`esptool` は DTR/RTS を操作してブートローダへ入るので、接続イベントのたびにファームウェアが再起動します。WCH-Link の attach はコアを保持して解放するので、再起動ではなくタイミングの中断になります。いずれにせよ本プロジェクトは開発環境向けのツールであり、環境の安定性が求められる場面には適しません。
- ディスクリプタのテーブルは、最後にマージした時点のもの、かつ upstream のボードリストが網羅する範囲でしかありません。どちらにも載っていないボードは、従来どおり通信によって識別されます。
- ディスクリプタが示すのは機種であって個体ではありません。USB シリアル番号を報告しないボード（Arduino UNO R3 の多くのリビジョンなど）は、`esptool` を遠ざけるには十分に認識されますが、安定した名前で公開することはできません。
- ファミリは、そのエントリのプラットフォームが名乗るアーキテクチャから推定しています。1 つのアーキテクチャ配下に複数のシリコンファミリを混在させるパッケージがあれば、そのボードは誤ったファミリに割り当てられ、そこに含まれる Espressif のボードは `esptool` へ回らなくなります。
- 汎用 USB-UART ブリッジの ID しか名乗らないボードは、構造上まったく識別できません。現時点の該当例が Sony Spresense です。
- 表を引いてターゲットに名前を付けるため、表に載っていないチップはシリーズ名、あるいは生のシグネチャにフォールバックします。あとで表に追加するとリンク名は変わります。これは attach をまたぐ変化ではなくアップグレードをまたぐ変化です。
- ベンダインタフェースを既にデバッグセッションが掴んでいるプローブには、ターゲットについて尋ねられません。プローブ自身はディスクリプタから名前が付くので、次の接続イベントまでそのポートのリンクは通常より少なくなります。
- 同じノード名がカーネルから別のデバイスへ再割り当てされたあとでは、古くなったリンクを検出できません。この場合は `cleanup` ではなく、そのポートに対する次の `publish()` で解消されます。
- 2 つのポートが同じボード ID を報告した場合、リンクは 1 つを共有し、最後に公開したものが残ります。古いポートは自分がまだ保持しているリンクを維持し、1 つも保持しなくなった時点で次の cleanup が状態を削除します。

[識別子の形式](identifier-format.ja.md)、[プローブの追加](adding-a-probe.ja.md)、[運用](operations.ja.md) も参照してください。

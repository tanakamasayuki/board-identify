# 変更履歴

*[English](CHANGELOG.md) | [日本語](CHANGELOG.ja.md)*

## 未リリース

- Espressif 識別の初期実装を追加。
- udev と systemd の統合例を追加。
- `remove` サブコマンドと全体オプション `--runtime-dir` を追加。
- esptool 4/5 のサブコマンド改名に追従し、ツール出力を色なし・折り返しなしに固定。
- ターゲットの MAC 読み取りで `BASE MAC:` より `MAC:` 行を優先。
- 同じポートを別のボード ID で再公開する際に、以前のリンクを削除。
- 切断されたポートの状態と対応するリンクをまとめて削除。
- 英語と日本語のドキュメントを併記。
- 標準の代替手段（`/dev/serial/by-id`、`by-path`、udev ルール）と、それを優先すべき条件を記載。
- 想定環境である WSL + USB/IP について、Windows 側のバス ID が Linux から見えないこと、`by-path` が attach 順に従うことを記載。
- プローブがボードを乱すこと、本ツールが開発環境向けであることを明記。
- USB/IP のオートアタッチのトレードオフ（順不同の attach か、固定順の手動 attach か）と、
  ときどき外れる attach をオートアタッチが復帰させる点を記載。
- WCH-Link デバッグプローブを sysfs の USB ディスクリプタから識別。USB 通信もポートのオープンも
  発生しない、初の非侵襲な識別パス。プローブの UART に対して `esptool` が走る前に連鎖を終える。
- WCH-Link の先の WCH RISC-V ターゲットを、チップのシグネチャと工場出荷時 UUID から識別。
  シグネチャが既知なら型番で命名し、不明ならシリーズ名、さらに生の 16 進へフォールバックする。
- ポートが持つ同一性ごとにリンクを公開。デバッグプローブとデバッグ端子の先のボードが、
  同じ tty を指すそれぞれの名前を持つ。`publish()`、`identify_port()`、`Probe.identify()` は
  リストを扱うようになり、状態ファイルは単一の `board_id` ではなく `board_ids` を記録する。
- 再公開時に削除するのは、そのポートが名乗るのをやめた名前だけに限定。プローブからターゲットを
  外してもプローブ自身のリンクは残る。
- `identify --no-target-probe` を追加。USB ディスクリプタのみを使い、USB 通信を一切行わない。
- ポートの USB メタデータをポートを開かずに sysfs から読む `usbinfo` を追加。
- `identify --json` の出力を、単一の `port` の下に識別結果のリストを並べる形に変更。
- WCH-Link がターゲットの壊れた読み取り値を抱えた状態から復旧。family バイトは正しいまま、
  チップ ID と UUID が同じ語の繰り返しになる症状。繰り返しの応答を拒否し、シグネチャが
  どのチップにも解決しない場合はプローブに見直させて読み直す。CH32V003 に対する
  probe-rs 0.32 の read だけで再現。detach でもターゲットの電源再投入でも解消しない
  （おかしいのはプローブ側）。復旧にはターゲットをリセットせずに解消する `81 0d 01 03` を
  使い、リセットによって解消する `wlink reset` は使わない。
- 依存に `pyusb` を追加。

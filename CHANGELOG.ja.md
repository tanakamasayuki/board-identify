# 変更履歴

*[English](CHANGELOG.md) | [日本語](CHANGELOG.ja.md)*

## 未リリース

- Espressif 識別の初期実装を追加。
- udev と systemd の統合例を追加。
- `remove` サブコマンドと全体オプション `--runtime-dir` を追加。
- esptool 4/5 のサブコマンド改名に追従し、ツール出力を色なし・折り返しなしに固定。
- ターゲットの MAC 読み取りで `BASE MAC:` より `MAC:` 行を優先。
- 同じポートを別のボード ID で再公開する際に、以前のリンクを削除。
- 切断されたポートの状態と対応するリンクをまとめて削除。
- 英語と日本語のドキュメントを併記。
- 標準の代替手段（`/dev/serial/by-id`、`by-path`、udev ルール）と、それを優先すべき条件を記載。
- 想定環境である WSL + USB/IP について、Windows 側のバス ID が Linux から見えないこと、`by-path` が attach 順に従うことを記載。
- プローブがボードを乱すこと、本ツールが開発環境向けであることを明記。
- USB/IP のオートアタッチのトレードオフ（順不同の attach か、固定順の手動 attach か）と、
  ときどき外れる attach をオートアタッチが復帰させる点を記載。
- WCH-Link デバッグプローブを sysfs の USB ディスクリプタから識別。USB 通信もポートのオープンも
  発生しない、初の非侵襲な識別パス。プローブの UART に対して `esptool` が走る前に連鎖を終える。
- WCH-Link の先の WCH RISC-V ターゲットを、チップのシグネチャと工場出荷時 UUID から識別。
  シグネチャが既知なら型番で命名し、不明ならシリーズ名、さらに生の 16 進へフォールバックする。
- ポートが持つ同一性ごとにリンクを公開。デバッグプローブとデバッグ端子の先のボードが、
  同じ tty を指すそれぞれの名前を持つ。`publish()`、`identify_port()`、`Probe.identify()` は
  リストを扱うようになり、状態ファイルは単一の `board_id` ではなく `board_ids` を記録する。
- 再公開時に削除するのは、そのポートが名乗るのをやめた名前だけに限定。プローブからターゲットを
  外してもプローブ自身のリンクは残る。
- `identify --no-target-probe` を追加。USB ディスクリプタのみを使い、USB 通信を一切行わない。
- ポートの USB メタデータをポートを開かずに sysfs から読む `usbinfo` を追加。
- `identify --json` の出力を、単一の `port` の下に識別結果のリストを並べる形に変更。
- WCH-Link がターゲットの壊れた読み取り値を抱えた状態から復旧。family バイトは正しいまま、
  チップ ID と UUID が同じ語の繰り返しになる症状。繰り返しの応答を拒否し、シグネチャが
  どのチップにも解決しない場合はプローブに見直させて読み直す。CH32V003 に対する
  probe-rs 0.32 の read だけで再現。detach でもターゲットの電源再投入でも解消しない
  （おかしいのはプローブ側）。復旧にはターゲットをリセットせずに解消する `81 0d 01 03` を
  使い、リセットによって解消する `wlink reset` は使わない。
- 依存に `pyusb` を追加。
- USB の VID/PID からボードを識別。テーブル `arduino_ids` としてコミットして保持する。
  `scripts/generate_usb_ids.py` が、`arduino-cli board details` の公開ダンプである
  `board_details.json` からマージする。ローカルの Arduino インストールを読まないのは意図的で、
  `~/.arduino15` にボードが現れるのはコアをインストールした後だが、識別したいボードとは
  まだセットアップしていないボードそのものだから。825 組、うち 677 組がボードを特定する。
- マージは追加のみ・VID → PID 順にソートして保存。手で直した内容はその後の実行でも残り、
  一度公開されたボードは upstream の改名後も同じ名前を保ち、変な位置に手で足した行は
  弾かれずに整列される。特定の組み合わせを忘れさせるには `GENERIC_BRIDGE_IDS` に追加する。
- そのテーブルと USB シリアル番号からボードを命名する `UsbDescriptorProbe` を追加。
  USB 通信も tty のオープンもリセットも発生しない。Arduino UNO R4 WiFi は sysfs だけから
  `arduino-uno-r4-wifi-<シリアル>` として公開される。
- VID/PID を他ファミリのボード定義が名乗っているポートでは `esptool` を実行しない。
  Arduino UNO や Nano Every が、ディスクリプタで判る事実を確かめるためだけに、挿すたび
  ブートローダへ落とされることがなくなる。未知の組み合わせは従来どおり `esptool` に回り、
  汎用 USB-UART ブリッジの先のボードも同様。
- 汎用 USB-UART ブリッジの ID にその先のボードを代弁させない。CH340、CP2102、FT232、
  PL2303 の組み合わせはマージ時に除外し、参照時にも再度拒否する。判定は両方向に対称で、
  そうしたポートはテーブルから命名もされず、`esptool` から遠ざけられもしない。Sony Spresense
  は CP2102 の標準 ID `10c4:ea60` を自分のものとして登録しているため、この除外がなければ
  すべての CP2102 ボードが Spresense として公開され `esptool` に到達しなくなる。
- Espressif と他ファミリの両方が名乗る組み合わせは除外する。テーブルから外すことが、
  `esptool` にポートを開けておくことになるため。シリコンではなくコアの名前が付いた
  アーキテクチャ（`mbed`、`zephyr`、`host`）は争わずに負けるので、`mbed` と `nrf52` の
  両方でパッケージされた Seeed XIAO nRF52840 は `nrf52` に解決する。同一ファミリの複数
  ボードが共有する組み合わせは、ファミリだけを残して名前を落とす。
- テーブルが名前を持っていても Espressif のボードは `esptool` に任せる。eFuse MAC は
  ブリッジチップの交換より長生きする識別子であり、どのプローブが先に当たったかで
  ボードの名前が変わってはならないため。
- `normalize_component()` でアクセントを句読点扱いせず除去。`Arduino Yún` は
  `arduino-y-n` ではなく `arduino-yun` になる。
